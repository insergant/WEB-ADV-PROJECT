# ---- Server ----
PORT=5000

# ---- Database (XAMPP / MySQL) ----
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=scout_db
DB_PORT=3307

# ---- Auth ----
JWT_SECRET=change_this_to_a_long_random_string

# ---- Email (OPTIONAL) ----
# Leave blank to run password reset in DEV mode (code is printed to the server console).
# Fill these in to actually email reset codes.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
